import { Injectable } from "@angular/core";
import { Registry } from "./registry.model";

@Injectable({
    providedIn: 'root'
})
export class RegistrationService {

    constructor() { }

    registerUser(registry: Registry) {

    }
}